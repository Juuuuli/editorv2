# Agentic SDLC 防呆與開發規則

1. **嚴格執行階段四 (Phase 4)**：
   在執行 `agentic-sdlc` 技能的第四階段時，規格書 (system_spec) 定義為「**開發後的檢驗證明**」。
   - **絕對禁止**未寫程式碼即捏造規格書。
   - 必須遵守順序：先寫 Code -> 依據寫好的 Code 讀取 `templates/system_spec.md` 範本 -> 產出該功能的檢驗規格書 -> 與使用者確認成果。

2. **絕對功能隔離**：
   嚴禁在單一檔案內混雜多個模組的邏輯，所有工具與面板功能都必須獨立存放於 `src/features/[功能名稱]` 之下。

3. **開發環境限制**：
   啟動伺服器時，必須強制確保 Vite 的 Port 綁定在 **5176**，以避免與系統其他開發服務發生衝突。

4. **UX 與互動行為規則 (UI/UX Guidelines)**：
   - **平移視角 (Panning)**：平移功能必須帶動整個畫布視角 (Viewport)，包含白底工作板一起移動，而不是只移動畫布內的物件。
   - **選取樣式 (Selection)**：選取物件時，必須使用客製化的紅色虛線選取框。
   - **智慧面板切換 (Auto-switch)**：當加入新物件到畫布，或是使用者單選畫布上的任何物件時，左側標籤必須「自動跳轉」至「屬性 (Properties)」面板，減少使用者切換的步驟。
