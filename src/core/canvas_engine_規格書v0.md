# 畫布引擎核心 (CanvasEngine) 開發規格書 (v0)

> **說明**：開發完成後，Agent 必須自我檢驗產出此文件，存放在 `src/core/` 底下。**若未來有修改，必須產出新版 (如 v1, v2...)，絕對不可覆寫原檔。**
> **建立日期**：2026-07-26 20:26:00

## 1. 規範檢視與符合程度
| 檢驗項目 | 標準規範/功能規則要求 | 實際開發狀況 | 符合程度 (Pass/Fail) |
| :--- | :--- | :--- | :--- |
| 架構位置 | 需放在 `src/core/` 且封裝為獨立 Class | 已封裝為 `CanvasEngine.js` 類別並存放於正確位置 | Pass |
| 邊界限制 (撞牆) | 物件移動 (object:moving) 不可超出白色畫布 | 實作 `enforceBoundingBox` 方法，精準過濾 `left` 與 `top` | Pass |
| 背景自適應 | 匯入底圖時不可選取，且畫布須等比縮放 | 實作 `setBackgroundImage`，設定 `selectable: false` 與 `setWidth` | Pass |
| 視覺隱藏控制點 | 契合手繪風格，隱藏預設藍色外框與方角 | 實作 `applyCustomStyles`，改為深灰色圓角控制點 | Pass |

## 2. 實作差異與優化說明
無。目前實作完全符合 `canvas_engine_規則v0.md` 中的設計目標，並成功整合了 `fabric` 原生事件機制。

## 3. 🌟 測試結果與品質保證
- **測試情境 1 (向左拖曳撞牆) 測試結果**：通過。將物件強行往左側拖出時，`enforceBoundingBox` 成功將 `left` 修正並鎖死在 `0`。
- **測試情境 2 (右下角碰撞) 測試結果**：通過。物件的寬高與坐標總和超過畫布時，正確觸發修正，停在最邊緣。
- **測試情境 3 (背景自動縮放) 測試結果**：通過。載入圖片後，畫布容器順利 Resize 成與底圖完全吻合。

## 4. 🌟 新增環境依賴說明
- **新增的 npm 套件**：`fabric` (負責 Canvas 繪製核心), `vite` (前端開發伺服器), `tailwindcss` (CSS 框架)
- **新增的環境變數 (.env)**：無
