# 素材庫模組規則 (v0)

## 1. 模組定位
提供使用者存放自訂圖形、產生的 QR Code、以及去背後的貼圖素材，供重複拖曳使用。

## 2. 輸入與輸出
- **輸入**：從 IndexedDB 讀取素材清單，以及監聽「存入素材庫」按鈕的點擊事件。
- **輸出**：將素材拖曳至畫布時，計算拖放的相對方座標，向 `CanvasEngine` 發出 `CANVAS:ADD_ASSET` 事件。

## 3. 邊界條件與例外處理
- **儲存空間限制**：因使用 IndexedDB，須檢測單一素材圖檔大小不可超過 5MB，超出需提示錯誤。
