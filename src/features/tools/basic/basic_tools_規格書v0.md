# 基本工具 (BasicTools) 開發規格書 (v0)

> **說明**：開發完成後，Agent 必須自我檢驗產出此文件，存放在 `src/features/tools/basic/` 底下。
> **建立日期**：2026-07-26 20:38:00

## 1. 規範檢視與符合程度
| 檢驗項目 | 標準規範/功能規則要求 | 實際開發狀況 | 符合程度 (Pass/Fail) |
| :--- | :--- | :--- | :--- |
| 架構位置 | 需放在 `src/features/tools/basic/` | 封裝於 `BasicTools.js` | Pass |
| 狀態互斥 | 選取、平移、畫筆必須單選互斥 | 實作 `setMode`，重置狀態並切換 `isDrawingMode` / `selection` | Pass |

## 2. 實作差異與優化說明
目前實作直接調用 `canvasEngine.canvas` 進行狀態切換。畫筆模式預設使用 `#334155` 深藍灰色，寬度 5px 以契合手繪風格。

## 3. 🌟 測試結果與品質保證
- **測試情境 1 (切換為畫筆) 測試結果**：通過。點擊畫筆按鈕後，`isDrawingMode = true` 且 `selection = false`。
- **測試情境 2 (切換為平移) 測試結果**：通過。游標變成 `grab`，且取消框選功能。

## 4. 🌟 新增環境依賴說明
- **新增的 npm 套件**：無
- **新增的環境變數 (.env)**：無
