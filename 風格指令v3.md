# 風格指令 V3 (給 AI 的 Prompt)

以下為四種不同 V2 高質感風格的 AI 提示詞。這是一份專門寫給 AI 看的指令（Prompt）。
你可以將你想要使用的風格，其 `---` 之間的內容整段複製，貼給任何 AI（Claude、ChatGPT、Cursor…），接著描述你要做的畫面即可。技術中立，HTML/React/Vue/Tailwind 都適用。

## 1. Light Sketch (經典手繪)

---

你要用以下設計風格產出介面。這是一套**高對比、極簡且帶有專業手繪插畫感**的風格，請嚴格遵守。

## 色彩
- 全域背景：`#f8fafc`（極淺灰白，用來襯托純白卡片）
- 卡片/側邊欄/頂部背景：`#ffffff`（純白）
- 邊框與主要文字：`#334155`（深墨水藍）
- 陰影：純色硬陰影 `rgba(51,65,85,1)`
- 重點點綴色（Highlight）：`#fef08a`（淡螢光黃，用於 Active 狀態或強調按鈕）

## 三個不可妥協的特徵
1. **絕不使用擴散/模糊陰影**：所有的陰影必須是實心硬陰影（例如 `8px 8px 0px rgba(51, 65, 85, 1)`），嚴禁使用 Tailwind 的 `shadow-lg` 等預設模糊陰影。
2. **手繪感微圓角與粗框**：所有主要邊框必須是 `2px solid #334155`，並搭配手繪微圓角（例如 `border-radius: 255px 15px 225px 15px/15px 225px 15px 255px;` 或 Tailwind `rounded-xl`）。
3. **無襯線字體與透明度排版法**：全站使用無襯線字體，資訊層次不依賴五顏六色的文字，而是依賴字體的粗細與透明度（如次要文字使用純黑搭配 `opacity-60` 或 `opacity-70`、寬字距 `tracking-widest`、大寫 `uppercase`）。

## 互動特效 (Micro-interactions)
- **Hover (懸停)**：按鈕或卡片 Hover 時必須有輕微上浮（`-translate-y-1`）與陰影加深或發光回饋。
- **Active (點擊)**：點擊時必須有下沉的物理回饋（如 `translate-y-0` 或 `scale-95`），並讓硬陰影收合。

---

## 2. Pro Slate (專業冷灰)

---

你要用以下設計風格產出介面。這是一套**現代俐落、適合高密度數據閱讀的專業後台**風格，請嚴格遵守。

## 色彩
- 全域背景：`#e2e8f0`（冷灰色）
- 卡片/側邊欄背景：`#ffffff`（純白）
- 頂部 Header：`#1e293b`（深石墨黑，創造頂部視覺重量）
- 邊框與主要文字：極細實線 `#cbd5e1`，文字 `#475569` 或 `#0f172a`
- 重點點綴色（Highlight）：`#0284c7`（科技藍）

## 三個不可妥協的特徵
1. **極細的邊界切割**：所有組件與卡片的邊框一律是 `1px solid`，顏色使用 `#cbd5e1` 或 `#334155`，絕不使用粗線條。
2. **極簡的立體感**：採用扁平化設計（Flat Design），陰影最多只能使用極淡的 `0 1px 3px rgba(15,23,42,0.05)`，一般卡片盡量無陰影。
3. **冷靜理性的高頻操作環境**：嚴禁使用任何鮮豔的螢光色大面積鋪底，亮色（如科技藍）僅限用於最核心的行動按鈕（Primary CTA）。

## 互動特效 (Micro-interactions)
- **Hover (懸停)**：以背景顏色深淺變化為主（例如按鈕從 `#ffffff` 變為 `#f1f5f9`），取代明顯的立體位移。
- **Active (點擊)**：細微的背景加深與輕微的 `scale-[0.98]` 點擊回饋。

---

## 3. Dark Geek (深色極客)

---

你要用以下設計風格產出介面。這是一套**專為長時間沉浸式工作設計的深色極客**風格，請嚴格遵守。

## 色彩
- 全域背景：`#090d16`（極深黑）
- 卡片/側邊欄/頂部背景：`#0f172a` 或 `#1e293b`（深藍黑，用來建立層級）
- 邊框：`1px solid #334155`
- 文字：`#f8fafc`（高對比亮白）或 `#94a3b8`（次要灰白）
- 陰影：深黑色硬陰影 `#020617`
- 重點點綴色（Highlight）：青色 `#38bdf8` 或是低調紫色 `#c084fc`

## 三個不可妥協的特徵
1. **嚴禁擴散的霓虹發光**：過去深色模式常犯的錯誤是濫用發光邊框，這會造成嚴重的視覺疲勞。此風格要求邊框為實線且無發光（無 `box-shadow` 光暈），維持銳利度。
2. **暗色階層明確切割**：利用背景的極深黑（`#090d16`）與卡片的深灰藍（`#1e293b`）來切分區塊，取代過度依賴線條框列。
3. **柔和的高亮設計**：當需要高亮背景或選中狀態時，使用極低透明度的色塊（如 `rgba(56, 189, 248, 0.15)`），不要使用高彩度實心色塊。

## 互動特效 (Micro-interactions)
- **Hover (懸停)**：按鈕邊框稍微提亮（如變為 `#475569`），或使用深黑硬陰影產生微幅的浮雕感。
- **Active (點擊)**：向右下方下沉的物理按壓感。

---

## 4. Retro Warm (復古文青)

---

你要用以下設計風格產出介面。這是一套**充滿溫潤手帳感與復古羊皮紙質感**的風格，請嚴格遵守。

## 色彩
- 全域畫布背景：`#ede2cd`（牛皮紙暖色）
- 卡片/側邊欄背景：`#fdfbf7`（羊皮紙白）
- 頂部 Header：`#36261c`（深胡桃木色）
- 邊框與文字：`#85634b` 或 `#5c412f`（深褐色）
- 陰影：半透明深褐硬陰影 `rgba(133, 99, 75, 0.2)`
- 重點點綴色（Highlight）：`#b45309`（溫暖漸層橘）或 `#f59e0b`（琥珀色）

## 三個不可妥協的特徵
1. **完全排除冷色調與純黑/純白**：禁止使用 `#000000` 或 `#ffffff`，所有的白與黑都必須帶有暖色木質調或是紙張的黃褐偏色。
2. **溫潤的線條厚度**：邊框使用 `1.5px` 或 `2px` 的粗細，模擬復古印刷或手帳鋼筆的筆觸，並搭配 `10px` 或 `12px` 的圓角。
3. **陽光灑落的立體感**：所有的陰影必須使用帶有透明度的深褐色硬陰影（如 `3px 3px 0px rgba(133,99,75,0.25)`），這會創造出絕佳的實體手帳感。

## 互動特效 (Micro-interactions)
- **Hover (懸停)**：卡片與按鈕產生輕微上浮（`-translate-y-1`），深褐陰影範圍變大。
- **Active (點擊)**：陰影收合，卡片貼齊畫布的按壓物理回饋。

---
